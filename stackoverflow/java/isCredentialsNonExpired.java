public boolean isCredentialsNonExpired() {
    // Assuming checkPasswordExpiration() is a method that checks if the password is expired
    // This method needs to be defined in the class or inherited from a superclass
    return !checkPasswordExpiration();
}