// Assuming encryptData is a byte array and decryptData is also a byte array
// but without knowing the actual types, this is a guess based on typical usage.

import java.util.Arrays;

public class Main45 {
    public static void main(String[] args) {
        byte[] encryptData = {/* some encrypted data */};
        byte[] decryptData = {/* some decrypted data */};

        System.out.println(Arrays.toString(encryptData));
        System.out.println(new String(decryptData));
    }
}