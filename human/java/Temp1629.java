import java.util.Arrays;

public class Temp1629 {
    public static void main(String[] args) {
        byte[] key = new byte[20];
        Arrays.fill(key, (byte) 0xaa);
        
        // Output the filled array to verify
        System.out.println(Arrays.toString(key));
    }
}