public class Temp1003 {
    public static void main(String[] args) {
        HttpRequestResponseHolder requestResponseHolder = new HttpRequestResponseHolder();
        loadContext(requestResponseHolder);
    }

    public static void loadContext(HttpRequestResponseHolder requestResponseHolder) {
        // Placeholder implementation for demonstration purposes
        System.out.println("Context loaded with: " + requestResponseHolder);
    }
}

class HttpRequestResponseHolder {
    // Placeholder class for demonstration purposes
}