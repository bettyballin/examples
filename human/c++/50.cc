That is not C++ code, it's a Git command. However, I'll provide you with a C++ code that executes this command using the `system` function:

cpp
#include <iostream>

int main() {
    system("git clone http://pari.math.u-bordeaux.fr/git/pari.git");
    return 0;
}