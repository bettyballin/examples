
if (!empty($_POST['username']) && !empty($_POST['password'])) {
    // Validate username and password
    // For example, username might need to be alphanumeric and password might need to be a certain length
}

