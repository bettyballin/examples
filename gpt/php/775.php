
   ShareTokensTable
   ->id
   ->token (unique, random string)
   ->recording_id
   ->user_id (optional, if you want to track who shared the link)
   ->expires_at (optional, if you want the link to expire)
   
