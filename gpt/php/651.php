
// Using the Hash facade
$hashedPassword = Hash::make('your_plain_text_password');

// Or using the bcrypt helper function
$hashedPassword = bcrypt('your_plain_text_password');

// Output the hashed password
$hashedPassword

