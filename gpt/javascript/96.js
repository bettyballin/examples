
(function() {
    // Create a new Date object for the current time
    var now = new Date();
    
    // Format the time as a string
    var timeString = now.toLocaleTimeString();
    
    // Set this field's value to the current time
    event.target
