
$(element).on('mousedown', function(event) {
  // Perform the action only if the mousedown event is genuine.
  if (event.which === 1) { // Check if the primary button (usually the left) was clicked.
    // Your action here
  }
});

