
$(document).keyup(function(event) {
    if (event.keyCode == 27) { // 27 is the escape key
        // Your logic to close the video or handle the escape event
        console.log('Escape key pressed');
    }
});

